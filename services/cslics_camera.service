[Unit]
Description=Service for running the CSLICS camera
After=network.target

[Service]
User=$USER
Group=users
WorkingDirectory=/opt/cslics/cslics_ref_raas_vision_processor/
ExecStart=python3 -m cslics_vision_processor.client_vision_processor --broker-host $CSLICS_HOST --models-path /home/$USER/cslics_config/models/ --image-source PICAM --process_conf_path /home/$USER/cslics_config/camera_process_config.json --config_file_path /home/$USER/cslics_config/camera_config_seasim_rm9_2024.json
Restart=always
RestartSec=5

[Install]
WantedBy=multi-user.target
